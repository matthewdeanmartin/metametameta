# Bugs

No open bugs.

Previously tracked bugs (sync-check `--output` double-join, `read_about_file_ast`
missing annotated assignments, and emoji crashing non-UTF-8 consoles) are fixed;
see the `[Unreleased]` section of `CHANGELOG.md` and their regression tests in
`tests/`.
